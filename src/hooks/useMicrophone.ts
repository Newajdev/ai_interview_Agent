'use client';
import { useCallback, useEffect, useRef, useState } from 'react';
export function useMicrophone() { const [level, setLevel] = useState(0); const [error, setError] = useState<string | null>(null); const stream = useRef<MediaStream | null>(null); const frame = useRef<number | null>(null); const stop = useCallback(() => { stream.current?.getTracks().forEach((track) => track.stop()); stream.current = null; if (frame.current) cancelAnimationFrame(frame.current); setLevel(0); }, []); const start = useCallback(async () => { setError(null); try { stream.current = await navigator.mediaDevices.getUserMedia({ audio: true }); const context = new AudioContext(); const analyser = context.createAnalyser(); analyser.fftSize = 128; context.createMediaStreamSource(stream.current).connect(analyser); const data = new Uint8Array(analyser.frequencyBinCount); const tick = () => { analyser.getByteTimeDomainData(data); setLevel(Math.min(1, data.reduce((sum, value) => sum + Math.abs(value - 128), 0) / data.length / 35)); frame.current = requestAnimationFrame(tick); }; tick(); } catch (cause) { setError(cause instanceof Error && cause.name === 'NotAllowedError' ? 'Microphone permission was denied. Update your browser permission and try again.' : 'No microphone is available. Connect one and try again.'); } }, []); useEffect(() => stop, [stop]); return { level, error, start, stop }; }
