export function AudioMeter({ level }: { level: number }) { return <div className="flex h-20 items-center justify-center gap-1.5">{Array.from({ length: 18 }, (_, index) => { const distance = Math.abs(index - 8.5) / 9; const height = Math.max(14, (1 - distance) * (22 + level * 62)); return <span key={index} className="w-1.5 rounded-full bg-cyan-300 transition-all duration-100" style={{ height }} />; })}</div>; }
